# 模型包安装指南

模型包是 VetarAI 的「即装即用」本地模型：应用本体不内置这些模型，按需下载安装、随时卸载、可更换更新。目前提供两类：

| 模型包 | 用途 | 体积 |
|---|---|---|
| Qwen2.5 0.5B Instruct（Q4_K_M） | 本地对话 | 约 469 MB |
| SenseVoiceSmall 语音识别（INT8） | 语音转文字（录音 / 音频文件） | 约 230 MB |

模型包与 Ollama 模型并存，无需切换后端；在对话模型之间切换时，应用会自动释放上一个模型占用的内存。

---

## 方式一 · 网络安装（推荐）

1. 打开 **设置 → 模型包**，进入 **目录源设置**。
2. 添加以下目录地址并保存：

   ```
   https://raw.githubusercontent.com/zero11924065-dev/VetarAI/main/modelpacks/catalog.json
   ```

3. 回到模型包列表，会出现两个模型包，点击 **安装** 即可。
4. 安装弹窗中若出现「同时开启全量联网」勾选属正常现象：目录索引托管在 GitHub（境外源），需要勾选一次才能读取目录；模型文件本身从魔塔社区（国内站）直连下载。勾选一次即可。
5. 装完即可用，无需重启、无需切换后端。

## 方式二 · 本地安装（无代理 / 纯离线）

适合完全不能访问境外站点的环境。共四步：

**第 1 步：新建一个文件夹**

例如 `~/模型包`（路径随意，记住它，后面要用）。

**第 2 步：下载模型文件放进文件夹**

建议按下面的结构放（每个模型包一个子文件夹）：

```
模型包/
├── catalog.json                ← 第 3 步手写
├── sensevoice-small/
│   ├── model_quant.onnx
│   ├── am.mvn
│   ├── tokens.json
│   └── config.yaml
└── qwen25-05b-instruct-q4_k_m/
    └── qwen2.5-0.5b-instruct-q4_k_m.gguf
```

下载地址（均为国内可直接访问的站点）：

- 语音包（4 个文件）：魔塔社区 **iic/SenseVoiceSmall-onnx** 页面
  `https://modelscope.cn/models/iic/SenseVoiceSmall-onnx`
  在该页「模型文件」中逐个下载 `model_quant.onnx`、`am.mvn`、`tokens.json`、`config.yaml`。
- 对话包（1 个文件）：hf-mirror 的 **Qwen2.5-0.5B-Instruct-GGUF** 页面
  `https://hf-mirror.com/Qwen/Qwen2.5-0.5B-Instruct-GGUF`
  在该页文件列表中下载 `qwen2.5-0.5b-instruct-q4_k_m.gguf`。

**第 3 步：在文件夹里手写一个 catalog.json**

把下面内容完整复制，保存为 `模型包/catalog.json`（sources 用相对路径，相对本 catalog.json 所在目录解析）：

```json
{
  "version": 1,
  "packs": [
    {
      "pack_id": "sensevoice-small",
      "name": "SenseVoiceSmall 语音识别（INT8 量化）",
      "task": "asr",
      "format": "onnx",
      "driver": "onnxruntime",
      "version": "1.0.0",
      "description": "FunASR 团队 SenseVoiceSmall ONNX INT8 量化版。多语种语音识别（中/英/粤/日/韩），支持情感与事件标记。约 230MB，CPU 即可运行。",
      "size_bytes": 241581392,
      "min_app_version": "0.4.29",
      "homepage": "https://modelscope.cn/models/iic/SenseVoiceSmall-onnx",
      "license": "MIT",
      "sample_rate": 16000,
      "files": [
        {
          "path": "model_quant.onnx",
          "size_bytes": 241216270,
          "sha256": "21dc965f689a78d1604717bf561e40d5a236087c85a95584567835750549e822",
          "sources": ["sensevoice-small/model_quant.onnx"]
        },
        {
          "path": "am.mvn",
          "size_bytes": 11203,
          "sha256": "29b3c740a2c0cfc6b308126d31d7f265fa2be74f3bb095cd2f143ea970896ae5",
          "sources": ["sensevoice-small/am.mvn"]
        },
        {
          "path": "tokens.json",
          "size_bytes": 352064,
          "sha256": "a2594fc1474e78973149cba8cd1f603ebed8c39c7decb470631f66e70ce58e97",
          "sources": ["sensevoice-small/tokens.json"]
        },
        {
          "path": "config.yaml",
          "size_bytes": 1855,
          "sha256": "f71e239ba36705564b5bf2d2ffd07eece07b8e3f2bbf6d2c99d8df856339ac19",
          "sources": ["sensevoice-small/config.yaml"]
        }
      ]
    },
    {
      "pack_id": "qwen25-05b-instruct-q4_k_m",
      "name": "Qwen2.5 0.5B Instruct 对话模型（Q4_K_M）",
      "task": "chat",
      "format": "gguf",
      "driver": "llamacpp",
      "version": "1.0.0",
      "description": "阿里通义 Qwen2.5 0.5B 指令模型 GGUF Q4_K_M 量化版。轻量本地对话模型，中英文可用，约 469MB，适合 Apple Silicon 本地推理。",
      "size_bytes": 491400032,
      "min_app_version": "0.4.29",
      "homepage": "https://huggingface.co/Qwen/Qwen2.5-0.5B-Instruct-GGUF",
      "license": "Apache-2.0",
      "context_length": 32768,
      "files": [
        {
          "path": "qwen2.5-0.5b-instruct-q4_k_m.gguf",
          "size_bytes": 491400032,
          "sha256": "74a4da8c9fdbcd15bd1f6d01d621410d31c6fc00986f5eb687824e7b93d7a9db",
          "sources": ["qwen25-05b-instruct-q4_k_m/qwen2.5-0.5b-instruct-q4_k_m.gguf"]
        }
      ]
    }
  ]
}
```

**第 4 步：添加本地目录源并安装**

设置 → 模型包 → 目录源设置，添加（把路径换成你实际的绝对路径）：

```
file:///Users/你的用户名/模型包/catalog.json
```

保存后列表出现两个模型包，点安装。本地源直接拷贝安装，不经过网络联网检查，也不需要全量联网。

---

## 怎么用

**对话模型包**：打开推理面板，统一模型列表中带「模型包」徽标的就是模型包模型，与 Ollama 模型并列，直接选用即可。切换模型时应用自动释放上一个模型的内存，无需手动卸载。

**语音模型包**：装完即自动可用。会话输入框点话筒按钮录音，说完自动转成文字；也可以把音频文件拖进会话（或选择文件）转写成文稿。如果未安装语音包时点话筒或传音频，应用会弹窗引导你安装。

---

## 常见问题

**问：从魔塔社区等国内站下载，需要开「全量联网」吗？**

不需要。应用按域名判断境内外，`.cn` / `.com.cn` 的站点（如 modelscope.cn）在标准联网模式下直连。只有目录索引或模型源在境外（GitHub、Hugging Face 等）时，确认弹窗才会附「同时开启全量联网」勾选，勾选一次即可。

**问：支持哪些模型？**

- 对话：任意 GGUF 格式模型（由 llama.cpp 驱动，模型架构须为 llama.cpp 所支持）。
- 语音：SenseVoiceSmall 的 ONNX 版（该模型有专门实现的驱动）。
- 嵌入：bge-m3 已内置在应用里，无需安装。

魔塔社区上有十几种框架的模型，目前并不全都支持——应用是驱动式架构，当前驱动覆盖 GGUF 对话与 SenseVoiceSmall ONNX 语音两类；PyTorch / TensorFlow / safetensors 等其它框架暂不支持，新增框架意味着新增驱动，属于后续扩展方向。

**问：安装失败或转写异常，去哪看原因？**

日志在数据目录的 `logs/` 下，macOS 为 `~/.subagent/logs/`。下载中断会自动断点续传并重做 SHA256 校验，校验不过会重试其它下载源；反复失败时请把最新日志一并反馈。

**问：「卸载」和「禁用」有什么区别？**

禁用：模型文件保留在磁盘上，只是从可用列表里隐藏，随时可重新启用。卸载：删除模型文件、释放磁盘空间，之后想用需重新安装。

**问：模型包占多少磁盘？**

语音包约 230 MB（4 个文件合计 241,581,392 字节），对话包约 469 MB（491,400,032 字节），两个全装合计约 700 MB。
